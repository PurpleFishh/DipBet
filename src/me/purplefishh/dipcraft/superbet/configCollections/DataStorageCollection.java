package me.purplefishh.dipcraft.superbet.configCollections;

/**
 * For obtaining data and then store it in a class for easier and faster access
 */
public interface DataStorageCollection {
    /**
     * Loading the data from config in variables
     */
    void loadData();
}
